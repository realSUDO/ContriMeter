rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to upload and read chat files
    match /chat/{teamId}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
